export interface Coordinator {
  id: string;
  name: string;
  email: string;
  department?: string;
  phone?: string;
  role: string;
  avatarUrl?: string;
  createdAt: string;
  updatedAt: string;
}